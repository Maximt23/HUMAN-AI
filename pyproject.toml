[project]
name = "human-ai"
version = "0.1.0"
description = "An inspectable prototype for persistent, emotion-like autonomous agents."
requires-python = ">=3.11"

[project.scripts]
human-ai = "human_ai.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
